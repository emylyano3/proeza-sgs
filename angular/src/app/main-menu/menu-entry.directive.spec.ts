import { MenuEntryDirective } from './menu-entry.directive';

describe('MenuEntryDirective', () => {
  it('should create an instance', () => {
    // const directive = new MenuEntryDirective();
    // expect(directive).toBeTruthy();
  });
});
